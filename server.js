// Import all dependencies here
var express = require("express");

// Define the port that server will be listening on  
var PORT = process.env.PORT || 8080;

// Create an instance of the app by running express
var app = express();